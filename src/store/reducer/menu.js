import * as ActionTypes from '../action-types';


let menu = (state={
    menu:[],
},action)=>{
    switch(action.type){
        default:
            break;
    }
    return state;
}

export default menu;